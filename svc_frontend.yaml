apiVersion: v1
kind: Service
metadata:
  name: nginx-service-frontend
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80        # service port
      targetPort: 80  # container port
      nodePort: 30080 # optional (you can omit this to auto-assign)

